scName: poc
nameSuffix: test

vaultServer: https://{{ .Values.ingress.hosts.vault.host }}

ingress:
  hosts:
    main:
      host: <host1>
      loadBalancerIP: <ip>
  LoadBalancerResoureGroup: <resource_group>
  certmanagerissuer: letsencrypt-staging
  # className: ingress-traefik
