apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: argowf-server
  namespace: tooling
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`service-poc.westeurope.cloudapp.azure.com`) && (PathPrefix(`/argo`) || PathPrefix(`/api`))
      services:
        - name: argo-argo-workflows-server
    
  tls:
    secretName: default-tls

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: default-tls
  namespace: tooling
spec:
  dnsNames:
    - service-poc.westeurope.cloudapp.azure.com
  secretName: default-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer