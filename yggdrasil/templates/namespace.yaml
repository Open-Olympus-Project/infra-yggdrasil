
{{- $root := . -}}
{{- range $path, $_ := .Files.Glob "services/**/config.yaml" }}
    {{ $project := $root.Files.Get $path | fromYaml }}
---
apiVersion: v1
kind: Namespace
metadata:
  name: {{ $project.namespace }}
{{- end }}
